
import * as AspectRatioPrimitive from "@radix-ui/react-aspect-ratio"

/**
 * AspectRatio es un componente que mantiene una relación de aspecto constante
 * cuando sus dimensiones cambian. Útil para imágenes, videos y otros contenidos
 * donde se necesita mantener una proporción específica.
 *
 * @example
 * <AspectRatio ratio={16/9}>
 *   <img src="..." alt="..." />
 * </AspectRatio>
 */
const AspectRatio = AspectRatioPrimitive.Root

export { AspectRatio }
